'use strict';

/**
 *  subfield-value controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subfield-value.subfield-value');
